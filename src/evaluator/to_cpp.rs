use std::collections::HashMap;

pub fn init_types() -> HashMap<String, String> {
    let mut hashmap = HashMap::new();
    hashmap.insert("".to_string(), "".to_string());

    hashmap
}

pub fn translate_type(str_type: &str) {

}
